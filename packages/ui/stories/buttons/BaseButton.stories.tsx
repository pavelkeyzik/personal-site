import React from 'react';
import { BaseButton } from '../../src/buttons/BaseButton';

export default {
  title: 'BaseButton',
};

export function Default() {
  return <BaseButton color="red">Text</BaseButton>;
}
